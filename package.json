{
  "name": "ppe_convert_pdf",
  "version": "1.0.0",
  "description": "PDF conversion api for Project portal",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx functions-framework --debug-brk --target=app [--signature-type=http]"
  },
  "author": "Jordan Schultz",
  "license": "ISC",
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.1.2"
  },
  "dependencies": {
    "express": "^4.18.1",
    "puppeteer": "^17.1.2"
  }
}
